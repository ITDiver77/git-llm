# GIT-READY IMPLEMENTATION REQUEST

## PROJECT CONTEXT
{project_context}

## YOUR TASK: Provide Git-ready Implementation
Transform the requested changes into a format that can be directly integrated into Git.

## OUTPUT REQUIREMENTS
You MUST provide your response in the following EXACT structure:

### 1. IMPLEMENTATION SUMMARY
Brief overview of changes (2-3 sentences)

### 2. CHANGE SPECIFICATIONS (JSON FORMAT)
```json
{
  "changes": [
    {
      "file_path": "src/main.py",
      "change_type": "modify|create|delete",
      "description": "Add error handling for API calls",
      "priority": "high|medium|low",
      "dependencies": ["file1.py", "file2.js"]
    }
  ],
  "total_files": 1,
  "estimated_effort": "small|medium|large",
  "requires_testing": true,
  "breaks_existing": false
}
### 3. UNIFIED DIFF PATCHES

For EACH modified file, provide a CLEAN unified diff patch:
```patch
--- a/src/main.py
+++ b/src/main.py
@@ -10,7 +10,13 @@
 def api_call(url):
     try:
         response = requests.get(url)
-        return response.json()
+        response.raise_for_status()
+        return response.json()
+    except requests.exceptions.RequestException as e:
+        logger.error(f"API call failed: {e}")
+        return None
+    except json.JSONDecodeError as e:
+        logger.error(f"Invalid JSON response: {e}")
+        return None
```
### 4. COMMIT MESSAGES

Provide 3 commit message options:

    Conventional format: feat: add error handling for API calls

    Descriptive format: Add comprehensive error handling to API call function

    Bulleted format: - Add try-catch for network errors\\n- Add JSON parsing error handling\\n- Add logging

### 5. BRANCHING STRATEGY

Suggestions:

    Branch name: feat/api-error-handling

    Base branch: main

    Merge strategy: squash or rebase

### 6. VALIDATION STEPS
```bash
# Test commands to verify changes
pytest tests/test_api.py -v
# Lint checks
black --check src/main.py
# Type checks (if applicable)
mypy src/main.py
```

IMPORTANT RULES

    ONLY provide patches for files mentioned in CHANGE SPECIFICATIONS

    Ensure patches apply cleanly (test with git apply --check)

    Include necessary imports in patches

    Maintain existing code style

    Add/update tests if applicable

    Consider backward compatibility
